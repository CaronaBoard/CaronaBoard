language: node_js
cache:
  directories:
    - $HOME/.npm
    - $HOME/.yarn-cache
    - node_modules
    - elm-stuff/build-artifacts
before_install:
  - npm install -g yarn
install:
  - yarn
script:
  - yarn ci
